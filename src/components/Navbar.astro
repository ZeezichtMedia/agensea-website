---

---

<!-- Skip to main content link voor toetsenbordgebruikers -->
<a
  href="#main-content"
  class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[2000] focus:bg-indigo focus:text-white focus:px-4 focus:py-2 focus:rounded-full focus:font-bold focus:shadow-lg"
>Direct naar inhoud</a>

<!-- Top Bar: Logo & Navigation Container -->
<!--
  Header bevat logo-pill (links) en nav-pill (rechts). De volledige header
  verbergt zich met translate-y bij scroll naar beneden, en komt terug bij
  scroll naar boven (Medium-pattern). Logo zit in een eigen frosted-glass
  chip in dezelfde stijl als de nav-pill, zodat het visueel rust geeft en
  niet meer "naked" over content drijft.
-->
<header
  class="fixed top-10 left-0 right-0 z-[900] pointer-events-none transition-transform duration-500 ease-out-expo"
  id="nav-header"
>
  <div class="max-w-7xl mx-auto px-6 flex justify-between items-center">
    <!-- Logo Pill -->
    <a
      href="/"
      aria-label="Agensea — naar homepagina"
      class="interactive pointer-events-auto inline-flex items-center bg-white/60 backdrop-blur-3xl border border-white/20 rounded-full shadow-[0_8px_32px_rgba(0,0,0,0.08)] px-4 md:px-5 py-2 md:py-2.5 transition-all duration-500"
      id="logo-main"
    ><img src="/brand/Agensea%20logo.svg" alt="Agensea" width="400" height="400" class="h-7 md:h-8 w-auto transition-all duration-300" id="logo-img" /></a>

    <!-- Logo Download Popup -->
    <div id="logo-download-popup" class="fixed z-[2000] hidden pointer-events-none">
      <div class="pointer-events-auto bg-white rounded-2xl shadow-2xl border border-warm-grey/30 p-5 w-72 animate-in">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-9 h-9 rounded-xl bg-indigo flex items-center justify-center">
            <svg class="w-4 h-4 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
          </div>
          <div>
            <p class="font-display font-bold text-dark text-sm">Logo nodig?</p>
            <p class="text-[10px] text-text-secondary">Download in het juiste formaat</p>
          </div>
        </div>
        <div class="space-y-2">
          <a href="/brand/Agensea logo.png" download="agensea-logo-full.png" class="flex items-center justify-between px-4 py-2.5 rounded-xl bg-cream hover:bg-indigo hover:text-white text-dark transition-all group text-sm font-medium">
            <span class="flex items-center gap-2">
              <svg class="w-4 h-4 text-indigo group-hover:text-white transition-colors" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>
              PNG
            </span>
            <span class="text-[10px] text-text-secondary group-hover:text-white/70 uppercase tracking-widest">Origineel</span>
          </a>
          <a href="/brand/agensea-logo-512.png" download="agensea-logo-512.png" class="flex items-center justify-between px-4 py-2.5 rounded-xl bg-cream hover:bg-indigo hover:text-white text-dark transition-all group text-sm font-medium">
            <span class="flex items-center gap-2">
              <svg class="w-4 h-4 text-indigo group-hover:text-white transition-colors" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>
              PNG
            </span>
            <span class="text-[10px] text-text-secondary group-hover:text-white/70 uppercase tracking-widest">512px</span>
          </a>
          <a href="/brand/agensea-logo-128.png" download="agensea-logo-128.png" class="flex items-center justify-between px-4 py-2.5 rounded-xl bg-cream hover:bg-indigo hover:text-white text-dark transition-all group text-sm font-medium">
            <span class="flex items-center gap-2">
              <svg class="w-4 h-4 text-indigo group-hover:text-white transition-colors" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>
              PNG
            </span>
            <span class="text-[10px] text-text-secondary group-hover:text-white/70 uppercase tracking-widest">128px</span>
          </a>
        </div>
      </div>
    </div>

    <!-- Navigation Pill (v9.1: Right Aligned & Scaled Up) -->
    <div class="pointer-events-none" id="nav-wrapper">
      <nav
        class="px-4 md:px-8 py-2.5 md:py-3 bg-white/60 backdrop-blur-3xl border border-white/20 rounded-full shadow-[0_8px_32px_rgba(0,0,0,0.08)] transition-all duration-500 pointer-events-auto flex items-center gap-4 md:gap-8"
        id="main-nav"
      >
        <!-- Primary Core Links -->
        <div class="flex items-center gap-4 md:gap-10">
          <a
            href="/#diensten"
            class="hidden md:block text-xs font-bold uppercase tracking-widest text-dark/60 hover:text-indigo transition-colors interactive"
            >Diensten</a
          >
          <a
            href="/#cases"
            class="hidden md:block text-xs font-bold uppercase tracking-widest text-dark/60 hover:text-indigo transition-colors interactive"
            >Cases</a
          >

          <button
            id="nav-search-btn"
            type="button"
            aria-label="Open AI zoeken"
            class="flex items-center gap-2 md:gap-3 text-xs font-bold uppercase tracking-widest text-dark/60 hover:text-indigo transition-colors interactive"
          >
            <svg
              width="14"
              height="14"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="3"
              aria-hidden="true"
            >
              <circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"
              ></path>
            </svg>
            <span class="hidden lg:inline">AI Zoeken</span>
          </button>
        </div>

        <!-- Divider -->
        <div class="w-[1px] h-5 bg-dark/10"></div>

        <!-- Menu Toggle Button -->
        <button
          id="menu-toggle"
          type="button"
          aria-label="Open menu"
          aria-expanded="false"
          aria-controls="mega-menu"
          class="relative group flex items-center gap-3 interactive hover:text-indigo transition-colors"
        >
          <div class="relative w-4 h-4" id="menu-dots">
            <span class="absolute top-0 left-0 w-1.5 h-1.5 rounded-full bg-current transition-all duration-500 ease-out-expo" id="menu-dot-1"></span>
            <span class="absolute top-0 right-0 w-1.5 h-1.5 rounded-full bg-current transition-all duration-500 ease-out-expo" id="menu-dot-2"></span>
            <span class="absolute bottom-0 left-0 w-1.5 h-1.5 rounded-full bg-current transition-all duration-500 ease-out-expo" id="menu-dot-3"></span>
            <span class="absolute bottom-0 right-0 w-1.5 h-1.5 rounded-full bg-current transition-all duration-500 ease-out-expo" id="menu-dot-4"></span>
          </div>
          <span
            id="menu-text"
            class="text-xs font-bold uppercase tracking-[0.2em] text-inherit transition-colors"
            >Menu</span
          >
        </button>
      </nav>
    </div>
  </div>
</header>

<!-- Mega Menu Overlay -->
<nav
  id="mega-menu"
  aria-label="Hoofdnavigatie"
  aria-hidden="true"
  class="fixed inset-0 z-[899] bg-cream/95 backdrop-blur-3xl pt-24 pb-20 md:flex md:flex-col md:justify-center overflow-y-auto overflow-x-hidden opacity-0 pointer-events-none transition-all duration-700 ease-out-expo scale-110"
>
  <!-- Decorative Background Blobs -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div
      class="mesh-blob w-[800px] h-[800px] bg-indigo/10 top-[-20%] right-[-10%] blur-[120px]"
    >
    </div>
    <div
      class="mesh-blob w-[600px] h-[600px] bg-violet/10 bottom-[-20%] left-[-10%] blur-[120px]"
    >
    </div>
  </div>

  <div
    class="relative z-10 w-full max-w-7xl mx-auto px-6 lg:px-10 mt-8 md:mt-0"
  >
    <div class="grid md:grid-cols-2 gap-16 lg:gap-32 items-center">
      <!-- Diensten (Key Activities) -->
      <div>
        <span
          class="inline-flex items-center gap-2 text-xs font-bold text-indigo uppercase tracking-[0.3em] mb-6 md:mb-12"
        >
          <span class="w-10 h-[2px] bg-indigo"></span>
          Wat we doen
        </span>
        <ul class="space-y-2 md:space-y-8">
          <li>
            <a
              href="/diensten/websites"
              class="mega-link group flex items-center text-5xl md:text-7xl font-display font-bold text-dark/20 hover:text-dark transition-all duration-500 hover:tracking-tight interactive"
            >
              Websites
              <span
                class="text-indigo opacity-0 -translate-x-8 group-hover:opacity-100 group-hover:translate-x-4 transition-all duration-500 hidden md:inline-block"
                >.</span
              >
            </a>
          </li>
          <li>
            <a
              href="/diensten/marketing"
              class="mega-link group flex items-center text-5xl md:text-7xl font-display font-bold text-dark/20 hover:text-dark transition-all duration-500 hover:tracking-tight interactive"
            >
              Marketing
              <span
                class="text-indigo opacity-0 -translate-x-8 group-hover:opacity-100 group-hover:translate-x-4 transition-all duration-500 hidden md:inline-block"
                >.</span
              >
            </a>
          </li>
          <li>
            <a
              href="/diensten/software"
              class="mega-link group flex items-center text-5xl md:text-7xl font-display font-bold text-dark/20 hover:text-dark transition-all duration-500 hover:tracking-tight interactive"
            >
              Software
              <span
                class="text-indigo opacity-0 -translate-x-8 group-hover:opacity-100 group-hover:translate-x-4 transition-all duration-500 hidden md:inline-block"
                >.</span
              >
            </a>
          </li>
        </ul>
      </div>

      <!-- Bureau Info -->
      <div class="py-4 mt-8 md:mt-0">
        <div class="space-y-8 md:space-y-12">
          <div>
            <span
              class="block text-[10px] font-bold text-text-secondary uppercase tracking-[0.3em] mb-4 md:mb-6"
              >Navigatie</span
            >
            <ul class="flex flex-col sm:grid sm:grid-cols-2 gap-3 md:gap-6">
              <li>
                <a
                  href="/over-ons"
                  class="mega-link text-xl font-bold text-dark hover:text-indigo transition-colors flex items-center gap-2"
                  ><div class="w-1.5 h-1.5 rounded-full bg-indigo/30 sm:hidden">
                  </div> Over ons</a
                >
              </li>
              <li>
                <a
                  href="/#cases"
                  class="mega-link text-xl font-bold text-dark hover:text-indigo transition-colors flex items-center gap-2"
                  ><div class="w-1.5 h-1.5 rounded-full bg-indigo/30 sm:hidden">
                  </div> Portfolio</a
                >
              </li>
              <li>
                <a
                  href="/contact"
                  class="mega-link text-xl font-bold text-dark hover:text-indigo transition-colors flex items-center gap-2"
                  ><div class="w-1.5 h-1.5 rounded-full bg-indigo/30 sm:hidden">
                  </div> Contact</a
                >
              </li>
              <li>
                <a
                  href="/kennisbank"
                  class="mega-link text-xl font-bold text-dark hover:text-indigo transition-colors flex items-center gap-2"
                  ><div class="w-1.5 h-1.5 rounded-full bg-indigo/30 sm:hidden">
                  </div> Kennisbank</a
                >
              </li>
            </ul>
          </div>

          <div
            class="p-6 md:p-8 bg-white/50 rounded-2xl md:rounded-3xl border border-white inline-block w-full"
          >
            <p
              class="text-[10px] font-bold text-text-secondary uppercase tracking-[0.3em] mb-2 md:mb-4"
            >
              Direct contact
            </p>
            <a
              href="tel:+31681383601"
              class="text-xl md:text-2xl font-display font-bold text-dark hover:text-indigo transition-colors"
              >+31 6 81 38 36 01</a
            >
            <p class="mt-1 md:mt-2 text-xs md:text-sm text-text-secondary">
              Vragen? We helpen je graag verder.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>

<script>
  const toggleBtn = document.getElementById("menu-toggle");
  const megaMenu = document.getElementById("mega-menu");
  const menuText = document.getElementById("menu-text");
  const dot1 = document.getElementById("menu-dot-1");
  const dot2 = document.getElementById("menu-dot-2");
  const dot3 = document.getElementById("menu-dot-3");
  const dot4 = document.getElementById("menu-dot-4");
  const mainNav = document.getElementById("main-nav");
  const navWrapper = document.getElementById("nav-wrapper");
  const links = document.querySelectorAll(".mega-link");
  let isOpen = false;
  let lastScroll = 0;

  // ── Menu Toggle Logic ────────────────────
  function toggleMenu() {
    isOpen = !isOpen;

    if (isOpen) {
      megaMenu?.classList.remove(
        "opacity-0",
        "pointer-events-none",
        "scale-110",
      );
      megaMenu?.classList.add(
        "opacity-100",
        "pointer-events-auto",
        "scale-100",
      );
      megaMenu?.setAttribute("aria-hidden", "false");
      toggleBtn?.setAttribute("aria-expanded", "true");
      toggleBtn?.setAttribute("aria-label", "Sluit menu");

      dot1?.classList.add("translate-x-[5px]", "translate-y-[5px]");
      dot2?.classList.add("-translate-x-[5px]", "translate-y-[5px]");
      dot3?.classList.add("translate-x-[5px]", "-translate-y-[5px]");
      dot4?.classList.add("-translate-x-[5px]", "-translate-y-[5px]");

      if (menuText) menuText.textContent = "Dicht";
      document.body.style.overflow = "hidden";
      if (window.lenis) window.lenis.stop();
    } else {
      megaMenu?.classList.add("opacity-0", "pointer-events-none", "scale-110");
      megaMenu?.classList.remove(
        "opacity-100",
        "pointer-events-auto",
        "scale-100",
      );
      megaMenu?.setAttribute("aria-hidden", "true");
      toggleBtn?.setAttribute("aria-expanded", "false");
      toggleBtn?.setAttribute("aria-label", "Open menu");

      dot1?.classList.remove("translate-x-[5px]", "translate-y-[5px]");
      dot2?.classList.remove("-translate-x-[5px]", "translate-y-[5px]");
      dot3?.classList.remove("translate-x-[5px]", "-translate-y-[5px]");
      dot4?.classList.remove("-translate-x-[5px]", "-translate-y-[5px]");

      if (menuText) menuText.textContent = "Menu";
      document.body.style.overflow = "";
      if (window.lenis) window.lenis.start();
    }
  }

  // Sluit menu met Escape voor toetsenbordgebruikers.
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && isOpen) toggleMenu();
  });

  toggleBtn?.addEventListener("click", toggleMenu);
  links.forEach((link) =>
    link.addEventListener("click", () => isOpen && toggleMenu()),
  );

  // AI Search Trigger
  document.getElementById("nav-search-btn")?.addEventListener("click", () => {
    window.dispatchEvent(new CustomEvent("open-ai-search"));
  });

  // ── Smart Scroll: shrink-on-scroll ────────────────
  // Header blijft altijd sticky. Voorbij de drempel krijgen logo-pill en
  // nav-pill een iets opaker witte achtergrond zodat ze beter "loskomen"
  // van de content eronder.
  const logoPill = document.getElementById("logo-main");
  const SHRINK_THRESHOLD = 80;

  function applyScroll() {
    const currentScroll = window.pageYOffset;
    if (currentScroll < SHRINK_THRESHOLD) {
      mainNav?.classList.remove("bg-white/80", "py-2", "px-5");
      mainNav?.classList.add("bg-white/60", "py-3", "px-6");
      logoPill?.classList.remove("bg-white/80");
      logoPill?.classList.add("bg-white/60");
    } else {
      mainNav?.classList.add("bg-white/80", "py-2", "px-5");
      mainNav?.classList.remove("bg-white/60", "py-3", "px-6");
      logoPill?.classList.add("bg-white/80");
      logoPill?.classList.remove("bg-white/60");
    }
    lastScroll = currentScroll;
  }

  let scrollTicking = false;
  window.addEventListener("scroll", () => {
    if (!scrollTicking) {
      requestAnimationFrame(() => {
        applyScroll();
        scrollTicking = false;
      });
      scrollTicking = true;
    }
  }, { passive: true });

  // ── Logo Download Popup ──────────────────
  const logoEl = document.getElementById('logo-main');
  const logoPopup = document.getElementById('logo-download-popup');

  if (logoEl && logoPopup) {
    logoEl.addEventListener('contextmenu', (e) => {
      e.preventDefault();
      const rect = logoEl.getBoundingClientRect();
      logoPopup.style.left = rect.left + 'px';
      logoPopup.style.top = (rect.bottom + 8) + 'px';
      logoPopup.classList.remove('hidden');

      // Close when clicking outside
      setTimeout(() => {
        document.addEventListener('click', function close(ev) {
          if (!logoPopup.contains(ev.target as Node)) {
            logoPopup.classList.add('hidden');
            document.removeEventListener('click', close);
          }
        });
      }, 10);
    });

    // Also close on Escape
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') logoPopup.classList.add('hidden');
    });
  }
</script>

<style>
  .ease-out-expo {
    transition-timing-function: cubic-bezier(0.16, 1, 0.3, 1);
  }

  .animate-in {
    animation: popIn 0.2s ease-out;
  }

  @keyframes popIn {
    from { opacity: 0; transform: translateY(-8px) scale(0.95); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

</style>
